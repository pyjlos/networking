name: Build

on:
  push:
    branches: [main]

jobs:
  build:
    uses: pyjlos/cicd/.github/workflows/build/go-build.yaml@v1.0.1
    with:
      go-version: "1.25"
      working-directory: "./httpserver/"